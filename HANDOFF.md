# Workout Tracker — Supabase Migration Handoff

Context for a fresh Claude Code instance picking up v1 (cross-device persistence) and preparing for v2 (AI-generated workouts from historical performance).

## Next session pickup

Current live version: **`v3.4.10`** (visible in bottom-right footer). `origin/main` is the source of truth; working tree is clean. **Through v3.4.10 fully shipped** — everything in v2.4.x (hydration cache, persistent coach history end-to-end, session-lifecycle recovery, ad-hoc dedup hardening), v2.5.x (adaptive coaching profile + analyze-time profile_updates, iterative plan refinement with multi-turn cache_control, hydrate parallelization, start-screen Generate path, Cardio Phase 1, rest timer floating pill, + Add Set carry-forward, Drop Sets Phase 1), v3.0.x (no-plan state as a first-class experience + history-edit mode), v3.1.0 (per-workout weight-mode toggle), v3.2.x (per-user AI model selection + Opus 4.7 temperature gating), v3.3.0 (cold-start plan generation), v3.4.0 (first-class supersets and giant sets — block-as-container plan JSON, per-workout `superset_groups` column, mid-session merge / separate / +Add round / per-block drag-reorder, opportunistic AI prescription), and the v3.4.x patch line (recent-workouts row click fix, workout-class library seeds, refine-in-no-plan fix, in-review block-aware swap, plan-id-scoped historical lookup, analyze-form input persistence, pre-session superset pairing, pre-session +Add Set / +Drop Set as session extras, and cascade-delete drop children) — all live at `www.sebvel.app`.

**Before editing code, read [`ARCHITECTURE.md`](ARCHITECTURE.md)** — it maps the repo layout, the JS module split (`js/resolver.js`, `js/data.js`, `js/ui.js`, `js/auth.js`, `js/app.js`, `js/models.js`), state ownership, cross-module call boundaries, and UI patterns. `index.html` is HTML+CSS+`<script src>` tags only; inline JS is gone since the 2026-04-19 refactor. Authoritative state of *what's shipped per version* lives in `ROADMAP.md` (Shipped sections, newest first); architecture/behavior decisions are in `DECISIONS.md`.

**v3.4.1 – v3.4.10 patch line (2026-05-04 → 2026-05-06).** Bug fixes and small UX improvements layered on top of v3.4.0 supersets, plus a coherent thread of work to make the active plan's day tab editable pre-session.

- **`v3.4.1`** — fix recent-workouts row click being clobbered by the History week render.
- **`v3.4.2`** — seed workout-class library entries (Barry's, Pilates, etc.) so resolver matches don't drop those sessions into orphan rows.
- **`v3.4.3`** — fix refine bailing when there's no active plan (the refine endpoint was rejecting cold-start refinement attempts).
- **`v3.4.4`** — block-aware exercise swap on the in-review plan (Generate → review screen). Pre-fix, swap on a superset member would corrupt the block container.
- **`v3.4.5`** — scope `loadHistorical` to the active plan. Pre-fix, selecting Day N on a brand-new plan rendered the most recent Day N workout from a *previous* plan as "historical" because the query filtered only on `(user_id, day_index)` — no plan_id scope. Added `.eq('plan_id', activePlanId)` mirroring the existing `loadDaysWithHistory` pattern, plus an early return when `activePlanId` is null.
- **`v3.4.6`** — preserve analyze-form inputs on "Use for next plan" chain. `renderGenerateInputs` hardcoded literal defaults (`value="60"` / `value="5"` / no checked, etc.) and never read `generatedInputs`, so chaining analyze → plan reset every field. Now reads `start_date` / `target_duration` / `training_days` / `history_weeks` / `include_photos` / `notes` from `generatedInputs` (when set), falling back to first-open defaults otherwise.
- **`v3.4.7`** — enable pre-session superset pairing on plan days. Two defects: (1) the `⟷` chain-link tap on a standalone card silently bailed because `openSupersetPicker` required state which is null pre-session — split the guard so the plan-day branch only requires the day's exercises array and passes `(state && state.exercises) || {}` to `groupRunsForRender`; (2) the chain-link on an AI-prescribed block member would have routed to "Pair" instead of "Remove" because `inBlock` derived only from `exState.supersetGroup`. Added `badgeLabel != null` as the primary signal — buildDay already passes a non-null badge (`'A1'`, `'A2'`, …) for block members and null for standalones, so it's the canonical "this card is a member of a block" indicator. Downstream `applySupersetMerge` / `applySupersetSeparate` already gate the `workouts.superset_groups` update on `todayPlanState.workoutId`, so DB writes are correctly skipped pre-session.
- **`v3.4.8`** — explored mutating `plan.data` on pre-session +Add Set so the new set became part of the prescription. Reverted in v3.4.9 per design feedback (pre-session adds should be session-scoped, not plan-permanent).
- **`v3.4.9`** — pre-session +Add Set / +Drop Set as session extras. `addExtraSet` and `addDropSet` lazy-init `todayState` + `exState` via `getOrInitToday` / `getOrInitExercise` (was bailing on null state). Pre-session adds are session-scoped extras (`isExtra: true`), kept in-memory until the session is started and the existing `toggleSet` cascade persists them — parent persists first, `parent.setId` set, child's `buildSetPayload` picks it up via `parentSetIdx` for the `parent_set_id` FK. Also fixed a pre-existing latent placement bug: `addExtraSet` used `.push()` which lands inside the prescribed range when `exState.sets` is sparse (e.g., user toggled only set #0 done on a 3-set prescription). Replaced with indexed assignment at `Math.max(prescribedLen, exState.sets.length)`; block-aware via `_flatEiToPlanMember`.
- **`v3.4.10`** — cascade-delete drop children + harden +Drop Set gating. After v3.4.9 enabled pre-session drops, deleting the parent extra left an orphan drop in-memory (parentSetIdx pointing at a deleted slot), the +Drop Set button stayed visible (gate was `length > 0` — sparse arrays with prescribed-range holes still passed), and a fresh tap would chain off a hole and land the new drop's parentSetIdx inside the prescribed range. Three coordinated fixes: (a) `deleteSet` cascade-deletes drop children of a non-drop parent (DB cascade was already wired via `parent_set_id ON DELETE CASCADE`; in-memory now splices the parent + collected drop indices in descending order, walks surviving drops to decrement `parentSetIdx` where the parent's array position shifted, and updates `set_order` on surviving persisted sets accounting for ALL removed indices). (b) `addDropSet` (both pre-session branch and `_addDropSetInner`) scans for the actual last populated index instead of using `length - 1`. (c) +Drop Set button gate flips from `exState.sets.length > 0` to `exState.sets.some(s => s != null)` — `.some()` skips holes, so the button hides when the array is all holes. Three render sites updated: `renderPlanDayExerciseCard`, `renderPlanDayExtraCard`, `renderAdHocExerciseCard`.

**v3.4.0 (2026-05-04) — First-class supersets and giant sets.** Block container `{superset:true, rest:int, exercises:[...]}` lives alongside regular exercises in `day.exercises[]`. New nullable column `workouts.superset_groups jsonb default '[]'` stores per-workout block structure (`[{exercise_orders:[int,...], rest:int}, ...]`) so history detail renders the block independent of `plan.data` mutations / plan deactivation. Renderers walk runs (`groupRunsForRender`) and emit `renderSupersetBlock` for contiguous same-group runs; standalone runs go through the same per-exercise card helper with `badgeLabel = null`. Mid-session pairing via the `⟷` chain-link icon in card headers (picker scoped to other-on-day items). Merge collapses two flat entries into a block, appends to an existing block, or concats two blocks. Tap inside a block to unpair — block dissolves at N=1. **+Add round** at block bottom adds a set to every member; rest timer fires only on the last-of-round done-tap (gated by `shouldFireRestForBlockMember`). **Drop sets inside members work unchanged** — cascade walks the member's local sets array. Per-block drag-to-reorder via SortableJS with unique `data-sort-zone="superset-<groupKey>"` so cross-block drops are rejected. Day-level block-as-unit reorder deferred (see ROADMAP "Known limitations to fix"); workaround is unpair → reorder → repair. AI prescribes opportunistically per a new `## SUPERSETS` section in `system-prompt-plan.md` (antagonist pairs, accessory finishers, time-constrained sessions; avoids heavy compounds and beginner cardio mixes). Coach Chat (`_formatPlanForCoach`, `getLiveContext`) and the AI prompt helpers (`formatCurrentPlan`) walk blocks and render them inline as `⟷ Member1 / Member2 / Member3 (Ns rest)`; live context tags member lines with `(superset)`. Spec / plan in `docs/superpowers/specs/2026-05-02-supersets-design.md` and `docs/superpowers/plans/2026-05-04-supersets.md`.

**v3.0.0 – v3.0.1 (2026-05-02) — End plan + no-plan UX as first-class experience.** Adds an "End plan" button to the Plans modal that flips `is_active = false` without replacing. Reversible via the existing Activate flow. Empty state (`#emptyState`) is now a real surface populated by `renderEmptyState`: three CTAs (Generate / Use a template / Blank session), View full History link, 5-most-recent Recent workouts list opening into the existing History detail modal. Day-picker dropdown shows a "No active plan or session" placeholder when empty. Start-screen overlay no longer auto-opens or forces the user in the no-plan branch — close button always available. Hydrate's no-plan branch now mirrors plan-hydrate Phase 1 (parallel-loads today's ad-hocs + exerciseLibraryById + locations) and applies a focus hierarchy: in-progress ad-hoc wins, else first ad-hoc of any state, else fall through to empty state. v3.0.1 specifically closed the ad-hoc-loading piece — pre-v3.0.1 a user with an in-progress ad-hoc + no plan saw the empty state on hard reload because `todayAdHocs` was never populated. New helpers: `endActivePlan` + `fetchRecentWorkouts` (data.js); `renderEmptyState` + `renderEmptyStateRecent` (ui.js); `recentWorkoutsCache` (ui.js state). No schema changes. Spec / plan in `docs/superpowers/specs/2026-05-02-end-plan-and-no-plan-ux-design.md` and `docs/superpowers/plans/2026-05-02-end-plan-and-no-plan-ux.md`.

**v3.0.2 (2026-05-02) — Auto-open start-screen in no-plan + ad-hoc state.** When the user lands in the no-plan branch with an ad-hoc focused (either on hydrate after reload, or after `onEndPlan` with an ad-hoc going), also auto-opens the start-screen overlay so the no-plan options (Generate / Use a template / Blank session) are surfaced alongside the focused session. Close button is always visible in the no-plan branch, so this is a gentle prompt rather than a forced wall. `onEndPlan` also gains the same focus hierarchy that hydrate uses: in-progress ad-hoc wins, else first ad-hoc of any state, else fall through to the empty state.

**v3.0.3 (2026-05-02) — Edit historical workouts (sets, RPE, notes) without restarting.** Pre-v3.0.3 the only ways to change a logged workout were Bring to today (which moves the whole session to today and resets the timer) or Discard session. Neither covers "I forgot to type the weight on set 2 / forgot to mark RPE / want to add a session note after the fact." Adds an Edit toggle to the history-detail header. In edit mode: set inputs (weight/reps or duration/distance for cardio), per-set done check, per-exercise RPE, per-exercise note, and workout-level session notes are all live. Auto-saves on change directly to DB; no Save button. New `historyEditMode` flag (resets on close/back). Five new persistence helpers in `data.js`: `historyUpdateSetField`, `historyUpdateSetDone`, `historyUpdateExerciseRpe`, `historyUpdateExerciseNote`, `historyUpdateWorkoutNotes`. Per-exercise note + RPE updates fan out to all sets in the exercise. Drop-set cascade intentionally NOT applied in history edit (each row toggles independently). Out of scope (handled by Bring to today): add/delete sets, swap exercise, drag-to-reorder, exercise-level delete.

**v3.3.0 (2026-05-03) — Cold-start plan generation.** Closes a v3.0.x design gap: the empty-state "Generate a plan" CTA can finally succeed. Plan-mode in `api/generate-plan.js` previously bailed with `400 "Import a plan before generating"` when there was no active plan, and again on no-history. Both bails are removed in v3.3.0. `formatCurrentPlan` is null-guarded; `buildUserMessage` injects a `COLD START` marker when both activePlan and history are empty so the planner explicitly builds a first plan from CLIENT PROFILE + USER INPUTS. Analyze keeps its no-history bail but the message now reads `"No workouts in the last N weeks. Try a wider history window or generate a fresh plan first."` (substitutes the chosen window). Refine and Swap unchanged — both fundamentally need an existing plan to operate on. Spec / plan: `docs/superpowers/specs/2026-05-03-cold-start-generate-design.md` and `docs/superpowers/plans/2026-05-03-cold-start-generate.md`.

**v3.2.1 (2026-05-03) — Gate `temperature` for Opus 4.7.** Patch on top of v3.2.0. Anthropic deprecated `temperature` (and `top_p` / `top_k` / `thinking`) on Opus 4.7 — extended-thinking-aware models return `400 invalid_request_error` if those parameters are included. Pre-v3.2.1, picking Opus 4.7 from any of the three model dropdowns produced a 502 "AI service unavailable" toast (server wraps the upstream 400). New `supportsTemperature` flag in `AVAILABLE_MODELS` plus a `modelSupportsTemperature(modelId)` helper in both `api/_models.js` and `js/models.js`. All 5 Anthropic POST sites (1 in coach-chat, 4 in generate-plan) now conditionally include `temperature` via spread. Sonnet 4.6 and Haiku 4.5 behavior unchanged. Forward note: if any code path later adds `top_p` / `top_k` / `thinking`, gate the same way.

**v3.2.0 (2026-05-03) — Per-user AI model selection.** New "AI models" section in the Coaching Profile screen (hamburger → Coaching Profile) with three dropdowns: Coach (default Haiku 4.5), Plan flows (default Sonnet 4.6 — covers plan/refine/swap), Analyze (default Sonnet 4.6). Selections persist in `coaching_profile.data` (`model_coach`, `model_plan`, `model_analyze`) — no schema migration. Each AI POST body sends `model:` resolved from the profile via three new helpers in `data.js` (`modelForCoach`, `modelForPlan`, `modelForAnalyze`); server validates via a shared allowlist (`api/_models.js` + `js/models.js`) and falls back to the bucket default on miss. New model releases: edit both files + redeploy. Spec / plan: `docs/superpowers/specs/2026-05-03-ai-model-selection-design.md` and `docs/superpowers/plans/2026-05-03-ai-model-selection.md`.

**v3.1.0 (2026-05-02) — Per-workout weight-mode toggle.** Tappable chip on each exercise card overrides `weight_mode` (Total ↔ Per side) for one workout placement, without editing the library. Solves the "same exercise, different machine at different gyms" case (e.g., Hammer Strength Incline plate-loaded at gym A, cable-driven at gym B). Editable in today's plan day, ad-hoc sessions, and the v3.0.3 historical-edit view; static label in read-only history detail. Hidden for bodyweight / none / cardio rows. New nullable `sets.weight_mode` column with check `('total', 'per_side')`; NULL = inherit from `exercises.weight_mode`. Fan-out keyed on `(workout_id, exercise_order)` via two helpers: `setExerciseWeightMode` (today/ad-hoc) and `historyUpdateExerciseWeightMode` (history-edit). Volume math (×2 for per-side), the "per side" hint, and CSV/JSON export all respect the override via a new `effectiveWeightMode(set, exerciseMeta)` resolver helper. New sets in a toggled placement inherit the override on `+ Add Set` / `+ Drop Set`. Two follow-on bug fixes landed: `createAdHocSession` / `deleteAdHocSession` / `onEndPlan` now toggle `#emptyState` / `#summaryBar` consistent with the six other sites that already did (no more empty-state-stacks-on-blank-session); chip handler in history-edit reads `historyDetails[wid].state.exercises[...]` (was missing the `.state.` accessor and silently no-op'd). Spec / plan in `docs/superpowers/specs/2026-05-02-weight-mode-toggle-design.md` and `docs/superpowers/plans/2026-05-02-weight-mode-toggle.md`.

**Session A (v1 + v1.1) fully shipped.** Everything from the original Session A plan is live: auto-populate on done-tap, session start/complete timer with Resume (`paused_ms`), exercise library + picker, ad-hoc off-plan sessions, multi-day-per-calendar, weight-mode display, delete affordances, historical session browser. All v1.1 known-limitations are closed.

**Session A tail polish (2026-04-17) landed after the original pickup was written:**
- Day-tab strip → native `<select>` dropdown with optgroups (plan days + ad-hoc) + standalone `+ New Session` button.
- Rest timer ±15s adjust buttons.
- Export reworked: date range + CSV or JSON, queries every workout in window (plan + ad-hoc).
- OTP-code auth flow (keeps sign-in inside the PWA on iOS home-screen installs); magic link still works as fallback. Requires `{{ .Token }}` in the Supabase Magic Link email template.
- `fmtP` now prefers `reps_range` over `reps_target` and never double-renders when both are populated.
- Per-exercise "view recent" modal on every card — last 5 prior sessions with sets/reps/weight/RPE.

**Post-Session A follow-ups (2026-04-19, v2.0.8 – v2.0.14):**
- `v2.0.8` — delete-session button for ad-hoc workouts.
- `v2.0.9` — `loadExerciseLibrary` now keys `exerciseLibraryByName` / `exerciseIdCache` by `normName(row.name)`, matching the contract the state declarations already documented. Seed rows are already lowercase so this is a no-op for them; guards against legacy mixed-case user-custom rows from pre-normalization-on-insert v1 code.
- `v2.0.10` — exercise name resolver. Plan exercise names are display labels ("Pull-ups (BW, full ROM)", "DB Incline Bench Press (30°)") and rarely match seed names exactly. `resolveLibraryRow(name)` generates an ordered list of candidate keys via deterministic transformations (paren-strip, hyphen↔space, depluralize) plus an `EXERCISE_ALIASES` constant, and returns the first candidate that's in `exerciseLibraryByName` — or null. Both the write path (`ensureExerciseId`) and the read path (`openExerciseHistory`) route through it, so plan-logged sets and ad-hoc/imported sets land on the same `exercise_id`. Library-side secondary hyphen index covers "plan has no hyphen / seed has hyphen" without changing the `normName` contract.
- `v2.0.11` — header consolidation. Three header action buttons (History, Import, Export) + the inline sign-out affordance collapsed behind a single `☰` button that opens a bottom-sheet menu reusing the existing history-modal pattern. Pure UI refactor; no schema or behavior changes. Design/plan specs in `docs/superpowers/specs/` and `docs/superpowers/plans/`.
- `v2.0.12` — session-level notes. Collapsible "Session notes" row between the session bar and first exercise card in both `buildDay` and `buildAdHocDay`. Save-on-blur upserts `workouts.notes` (column already existed from the init migration). Focus on the textarea triggers the existing `ensureWorkout` lazy-create path so pre-workout context (soreness, sleep) can be logged before the first set-done. Historical views render the textarea read-only. Feeds the v2 AI planner alongside set data for subjective recovery signals.
- `v2.0.13` — gym profiles. New `locations` table (id, user_id, name, created_at) with case-insensitive uniqueness per user and RLS; `workouts.location_id` nullable FK with `ON DELETE SET NULL`. Hamburger menu gains a "Gym Profiles" row that opens a CRUD modal (add / inline-rename / delete-with-count-hint). Session view gains a location dropdown between the session bar and session-notes row, defaulting to the most-recently-used gym; zero-gym users see a `+ Add a gym` prompt. Lazy workout creation carries a pre-start pick via a `pendingLocationId` sentinel (undefined = not touched, null = explicit no-gym, UUID = explicit gym). Read surfaces everywhere: historical day-picker (disabled dropdown), History browser list badge, History detail metadata line, View Recent per-exercise modal session header. Single migration `20260419000000_locations.sql`.
- `v2.0.14` — rest timer survives PWA backgrounding. Counter-based `setInterval` replaced with a wall-clock deadline (`restTargetMs`); remaining time is computed from `Date.now()` on every 250ms tick, so iOS suspending the interval no longer freezes the countdown. A `visibilitychange` catch-up fires `restComplete()` immediately if the app returns past the deadline. Added a gentle 880Hz `AudioContext` chime alongside the existing vibrate as a secondary completion cue. Notification API deliberately rejected for this feature — iOS backgrounds PWA JS, so `setTimeout(notify, …)` won't fire at the deadline without a service worker + Web Push architecture.
- `v2.0.15` — View Recent modal correctness. Two distinct bugs, both documented in detail in `MAJOR-BUGS.md`. (1) Fitbod CSV history was imported via SQL scripts pasted into the Supabase SQL Editor and one batch ran twice — ~1107 duplicate `sets` rows across ~22 "Fitbod Import" workouts, visible in View Recent as every set doubled (e.g. "0 × 12 · 0 × 12 · 0 × 12 · 0 × 12 · 0 × 7 · 0 × 7" instead of the 3 sets the user performed). (2) Orphan user-custom `db romanian deadlift` row from pre-`v2.0.10` `ensureExerciseId` behavior intercepted `resolveLibraryRow` before the alias reached the canonical seed, so all ~40 historical Fitbod sessions were stranded and the modal rendered "No prior sessions logged." Fix mix: data dedupe + orphan merge/delete + `cable crossover → cable fly` sets merge + plan JSON cleanup (renamed "Weighted Reverse Crunch" to "Reverse Crunch", removed vestigial VO2 Max / Zone 2 Walk entries). Code change: added `'db bench press' → 'dumbbell bench press'` alias. Schema hardening: new migration `20260419010000_sets_unique_position.sql` adding a partial unique index on `sets (workout_id, exercise_id, exercise_order, set_order) where done = true` to prevent a future re-run from silently double-inserting.
- `v2.0.18` — kg / lbs weight unit toggle. New hamburger item "Weight unit (lbs|kg)" flips display + input between the two units. Conversion helpers in `js/data.js` (`LBS_PER_KG`, `getWeightUnit`, `setWeightUnit`, `lbsToKg`, `kgToLbs`, `displayWeight`, `parseWeightInput`, `normalizePrescribedLbs`); input label in `renderSetRow` flips between `LBS`/`KG`; `fmtP` appends current unit to prescribed hints; `openExerciseHistory` uses `displayWeight` for historical set strings. `sets.weight` remains canonical lbs — no schema change, no migration. Preference persisted in `localStorage['weightUnit']` (device-local; cross-device sync via `user_settings` table deferred). Shipped for an upcoming Europe trip. Design + smoke-test checklist in `docs/superpowers/specs/2026-04-19-kg-lbs-toggle-design.md`; implementation plan in `docs/superpowers/plans/2026-04-19-kg-lbs-toggle.md`.
- `v2.0.17` — extra sets on prescribed exercises. Editable plan-day exercise cards now render a "+ Add Set" button beneath the prescribed rows. Added sets carry a per-set `isExtra` flag stamped at `addExtraSet` / `addExerciseToSession` / `stateFromWorkout`, render with a 3px accent left border plus a ✕ delete button, and write to `sets` with null `prescribed_weight` / `prescribed_reps` while reusing the prescribed exercise's `exercise_id`. Prescribed sets stay immutable from the UI (no delete, no shift). Progress totals and per-exercise status badges include extras. No schema change. Design + smoke-test checklist in `docs/superpowers/specs/2026-04-19-extra-sets-on-prescribed-exercises-design.md`; implementation plan in `docs/superpowers/plans/2026-04-19-extra-sets-on-prescribed-exercises.md`.
- `v2.0.16` — flexible session start. Replaced the silent auto-focus-on-hydrate default with an explicit bottom-sheet start modal. Three paths: rotation-based suggested day (`(lastCompletedDayIndex + 1) mod plan.days.length`, one SELECT per hydrate), pick from plan days (in-place expanding list with in-progress / completed-today badges), blank (ad-hoc) session. Modal auto-opens on hydrate when no in-progress session exists; always-available "Start another workout" hamburger item reopens it. In-progress sessions take priority over lowest-day-index in the focus hierarchy (a user who completed Day 2 and started Day 3 lands on Day 3 on reload). Tab dropdown appends `(in progress)` to any option that's started-but-not-ended. No-active-plan users see only the blank-session path + "import a plan" link; close button hidden in that state. Removed the now-redundant "+ New Session" standalone button. No schema changes. Design + smoke-test checklist in `docs/superpowers/specs/2026-04-19-flexible-session-start-design.md`; implementation plan in `docs/superpowers/plans/2026-04-19-flexible-session-start.md`.

**Session B shipped (2026-04-20, v2.0.19 – v2.0.25):**
- `v2.0.19` — weekly History browser. Replaced the flat per-workout list with a Sunday-Saturday week view: navigator (←/→ with earliest-workout gate), summary bar (sessions, days trained/planned, volume unit-aware, avg RPE, plan completion%), skipped-across-week callout, workout cards (purple accent for ad-hoc). Workout detail drill-down reuses existing `renderHistoryDetail`. New reusable `fetchWeekSummary(userId, weekStart, weekEnd)` query in [js/data.js](js/data.js) returns structured week data with per-workout aggregates (volume by weight_mode: per_side × 2, bodyweight = added load only, none = 0), prescribed-vs-completed ratios, and skipped-exercise detection. Same shape consumed by the AI Edge Function's `formatVerbatimHistory` server-side.
- `v2.0.20` — physique photos. New `physique_photos` table (see migration `20260420000000_physique_photos.sql`) + private Supabase Storage bucket `physique-photos` with path-prefix RLS (`{user_id}/{uuid}.{ext}`). Gallery in a three-view modal (gallery / upload / viewer). Rendering via time-limited signed URLs (1h TTL, cached in memory by path). Upload → storage + metadata row; on metadata insert failure, storage row is rolled back so orphans don't accrue. Delete removes storage first, then the metadata row. Photos feed into AI plan generation as multimodal image blocks.
- `v2.0.21` — Generate + Review UI for AI plans (Part 2c). New hamburger entry "Generate next week's plan" fires `/api/generate-plan`. Loading state with spinner + message for the ~30s call. Review overlay with coaching notes card, per-day breakdown (weight_mode-aware set summaries: `per_side` shows `/ea`, `bodyweight` shows `BW`/`BW+N`), Cancel + Accept buttons. Accept calls a new `savePlanAsActive` helper (extracted from `handleImport`) that writes to Supabase, flips `is_active`, and re-renders. Coaching notes ride along in the plan JSON blob — no schema change.
- `v2.0.22` — Plans management modal (Part 2c.5). New hamburger entry "Plans" lists every plan newest first with Active badge, workout count, created date, start date (if present). Per-row Activate (disabled on currently-active) and Delete (disabled if workout count > 0) buttons. Activate uses a new `activateExistingPlan` that flips `is_active` without creating a new row. Delete is gated by workout count to prevent orphaning sets. Also: `savePlanAsActive` now stamps `plan.start_date` with today's local date at save time; Edge Function's `fetchActivePlan` selects `created_at` as a fallback; `formatCurrentPlan` emits `Started: YYYY-MM-DD (N weeks ago)` plus per-day target durations; `formatWorkoutVerbatim` emits `Target X min · Actual Y min` for workouts on the currently-active plan. Generate modal gains a Cancel button during loading + guards against double-fire (re-surfaces existing loading modal instead of firing a duplicate fetch).
- `v2.0.24` — bug fixes: (1) tracker header week label was showing Claude's free-form emission ("Apr 20-24, 2026" Mon-Fri) while the History browser uses Sun-Sat. New `planWeekLabel(plan)` helper derives Sun-Sat from `start_date`; `savePlanAsActive` normalizes on save; render sites fall back to the derived label for plans saved before stamping via `ensureStartDate` injecting `plans.created_at`. Single source of truth: `start_date`. (2) On fresh page load, focusing Day 1 with no today-state showed an empty editable template even when the user had trained that day earlier in the week against an older `plan_id`. `loadHistorical` was already wired to the day-picker change handler but never fired on initial hydrate. Fixed: `hydrate` now auto-calls `loadHistorical(currentDay)` for plan-day tabs that lack a today-state — mirrors the day-picker handler.
- `v2.0.25` — pre-generate inputs form (Part 2c.6). Generate flow now opens to an inputs form before firing the API call. Three optional fields: `start_date` (defaults to next Sunday), `target_duration` (defaults to 60 min), `notes` (free-form). Edge Function parses the JSON body and folds non-null inputs into the user message as a `USER INPUTS FOR THIS WEEK` section. On Accept, the user's `start_date` overrides `savePlanAsActive`'s auto-stamp so plans generated Thursday for Sunday start get dated correctly. **System prompt gains a cached USER INPUTS section** that sets defaults, priority rules (user inputs override generic guidance), and explicit handling rules — moves the input-reconciliation reasoning into the cached prefix so Claude sees it once at cache write. Also added: 55s `AbortController` timeout on the Anthropic fetch so stuck upstream surfaces as a clean 504 toast instead of an infinite spinner, and server-side timing logs at each phase boundary for future diagnosis. See [MAJOR-BUGS.md](MAJOR-BUGS.md) (2026-04-20 entry) for the full debugging narrative — the "Respect any user inputs above" instruction sentence we initially added to dynText was causing ~1000 extra output tokens and tipping calls over the timeout.
- `v2.0.26` — cold-start UX + photo parallelization. Makes the Anthropic prompt-cache miss invisible to the user. **Frontend silent retry:** on network error / 5xx / 504, `submitGenerateInputs` automatically retries once against the now-warm serverless instance and Anthropic cache. Loading copy swaps from "Reviewing your training…" to "Still generating — warming up the AI…" on attempt 2. 4xx (no plan, auth, validation) and 200-without-plan pass straight through — those don't fix themselves on retry. User-initiated Cancel aborts both attempts cleanly via the existing `generateAbortController`. New `attemptGeneratePlan(token, signal)` helper in [js/ui.js](js/ui.js) classifies each attempt as `success` / `retry` / non-retriable `error`. **Server photo parallelization:** goal + progress downloads in [api/generate-plan.js](api/generate-plan.js) now run via `Promise.all` instead of sequentially (~200-500ms saved on the prompt-build phase). Content-block order preserved so the Anthropic cache prefix is unchanged. Rejected alternatives documented in [DECISIONS.md](DECISIONS.md) 2026-04-20 entry: query parallelization (already shipped), history-scope reduction (wrong diagnosis — output tokens dominate, not input), cron warm-up (Fluid Compute blunts the win; deferred to ROADMAP).
- `v2.0.27` — user-controlled plan inputs (Training days, History context, Include photos) + system-prompt cleanup. The Generate form gains three new controls: **Training days** (1-6, default 5) sets the exact number of day entries the AI emits; **History context** (1-12, default 4) sets how many weeks of workouts feed the prompt (effective verbatim weeks clamped to `min(2, N)` so short windows stay all-verbatim); **Include physique photos** checkbox (default unchecked) lets the user opt in per call — when off, the server skips the Supabase Storage fetch entirely (~1-3s faster, no image tokens). [api/generate-plan.js](api/generate-plan.js) now clamps all numeric inputs server-side (defense in depth), enforces `plan.days.length === trainingDays` in `validatePlan` (wrong count returns 422 instead of silent corruption), and drops the redundant "Match the current plan's day structure…" dynText line. [system-prompt.md](system-prompt.md) rewritten so the hardcoded "5-day Upper/Lower Sun-Thu" no longer appears in CLIENT PROFILE / OUTPUT FORMAT / HARD CONSTRAINTS — the USER INPUTS section is now the single source of truth for day count, and the priority rule explicitly overrides the historical preference. Four residual conflict-risk spots in COACHING PHILOSOPHY patched so Claude never interprets generic coaching guidance ("shorten the plan", "split re-evaluation", cut-phase duration) as license to contradict user inputs. **Photo-handling section intentionally unchanged** — already correctly conditional on *"may include attached images"* / *"If no photos are attached"*, so the opt-out checkbox works without prompt changes. One-time Anthropic cache invalidation on first call after deploy (~35-45s); warm thereafter.
- `v2.4.0` — Hydration cache for fast warm boot. On every lifecycle event (`visibilitychange → hidden`, `beforeunload`, successful `hydrate()`, plan activation), the app snapshots its in-memory tracker state to `localStorage['wt.hydration.v1']` — active plan, focused day, today's plan-states + ad-hocs, `daysWithHistory`. On next boot, a synchronous `paintFromCache()` IIFE in [js/app.js](js/app.js) runs before any network call: hides `#emptyState`, paints the plan title/week, day tabs, focused-day cards with logged sets. A small `Refreshing…` pill next to the week label signals the optimistic-paint state. `hydrate()` then runs as usual and reconciles: overwrites in-memory state with fresh server truth, removes the pill. User-ID mismatch (different account signed in) or plan-404 (active plan deleted elsewhere) clears the cache mid-reconcile and falls back to empty state. Sign-out clears the cache so the next account doesn't inherit it. Schema-versioned blob (`schemaVersion: 1`); age-gated at 7 days. No schema changes. Design: [docs/superpowers/specs/2026-04-21-hydration-cache-design.md](docs/superpowers/specs/2026-04-21-hydration-cache-design.md); plan: [docs/superpowers/plans/2026-04-21-hydration-cache.md](docs/superpowers/plans/2026-04-21-hydration-cache.md).
- `v2.3.2` — Triple-check audit of the three-way prompt split: strip plan-mode-specific input references from `system-prompt-core.md`. Pre-fix, core referenced `Training days` in three places (CLIENT PROFILE override, Execution rate parenthetical, Proactive re-evaluation split-structure wrapper) and `Target session duration` once (Phase awareness parenthetical). In analyze mode those inputs aren't in the user message, so the references dangled — the MAJOR-BUGS 2026-04-20 "instruction text has real reasoning cost" anti-pattern. Fixed: rephrased core's CLIENT PROFILE line to neutral ("established default; client's actual day count may differ for a given week"); dropped the three plan-specific parentheticals; rewrote the Split structure bullet without the Training-days wrapper. Plan suffix already covered every removed directive (Training-days split adaptation in USER INPUTS; day-count HARD CONSTRAINT; Target-duration override in USER INPUTS; historical-preference override in priority rule). Zero change to plan-mode behavior; analyze mode no longer sees dangling references. One-time Anthropic cache invalidation on first call after deploy.
- `v2.3.1` — Analyze mode gets multi-photo comparison. Pre-fix, analyze received at most 1 progress photo (plan-gen inherited default) and the prompt had a single vague line about "incorporate photo observations." Two coupled changes:
  - **`fetchPhysiquePhotos(userId, progressLimit)`** now parameterized. Plan-gen still gets 1 (unchanged). Analyze fetches up to 4 progress photos via new `ANALYZE_PROGRESS_PHOTO_LIMIT` constant.
  - **`buildAnalyzeUserMessage`** emits progress photos in chronological order (oldest → newest), each labeled *"PROGRESS photo N of M (YYYY-MM-DD)"* with the latest also tagged `(LATEST)`. Prompt structure makes the sequence unambiguous.
  - **Analyze prompt** (`system-prompt-analyze.md`) gets a dedicated PHYSIQUE PHOTO ANALYSIS section: required to compare latest-progress-vs-goal (which muscle groups are close to the goal, which lag) AND compare multiple progress photos over time (what visibly changed, what didn't). Single-photo case: analyze vs goal only, explicitly note over-time comparison unavailable. No-photos case: skip photo commentary entirely. Photo observations weave into `progressing` / `concerns` / `next_week` sections — no separate photo section.
- `v2.3.0` — Split plan generation from training analysis. Motivation: the user wanted deeper coaching commentary than the 60-word `coaching_notes` field allowed, AND wanted plan-gen faster by removing the reasoning overhead of embedded commentary. Coach Chat (Haiku, 2-week window) was wrong for depth. Solution: a separate analyze call on Sonnet with the same rich context as plan-gen, producing a four-section structured written assessment (`trends` / `progressing` / `concerns` / `next_week`). Plan-gen becomes structure-only.
  - **System prompt split into three bundled files** (`system-prompt-*.md` via `includeFiles` glob): `core.md` (CLIENT PROFILE + COACHING PHILOSOPHY + EXERCISE LIBRARY, shared), `plan.md` (plan-specific: USER INPUTS as programming constraints, plan JSON output format, HARD CONSTRAINTS), `analyze.md` (analyze-specific: USER INPUTS as analysis focus, four-section JSON output format). Cold start reads all three; assembles `SYSTEM_PROMPT_PLAN` and `SYSTEM_PROMPT_ANALYZE`. Each gets its own `cache_control` 1h TTL → independent Anthropic cache entries.
  - **`coaching_notes` field removed** from the plan output contract. `validatePlan` no longer requires it. Every "explain in coaching_notes / reference in coaching notes / flag in coaching notes" directive removed from the plan-gen prompt (~40 lines cut). Expected plan-gen latency saving: 5-10s (output token drop + reasoning-overhead elimination per the MAJOR-BUGS 2026-04-20 lesson).
  - **New `mode: 'analyze'` branch** on `/api/generate-plan` → `handleAnalyze`. Sonnet, 1000 max tokens. Same context builders as plan-gen (active plan, configurable history window, optional photos). Returns `{ analysis: { trends, progressing, concerns, next_week }, weeks_analyzed, ... }`. Target: 500-800 tokens output, ~15-25s warm.
  - **Input conflict audit** per the user's explicit ask (lesson from v2.0.27): analyze-mode payload strips `training_days`, `target_duration`, `start_date` client-side so they never reach the analyze prompt — no conflict with the analyze-mode USER INPUTS spec. Notes semantics deliberately differ between modes: plan-gen treats notes as programming constraints; analyze treats them as analysis focus / questions to answer. Core prompt audited to remove all `coaching_notes`-tied directives that would have dangled after the field was removed.
  - **"Use for next plan" chaining.** Analyze review has a button that formats the four sections with labels (`TRENDS: ... / PROGRESSING: ... / CONCERNS: ... / NEXT WEEK FOCUS: ...`) and stuffs them into plan-gen's notes field, switches back to the inputs form with all other inputs preserved. One click to chain analyze → plan with the analysis as coaching guidance.
  - **UI:** Generate form has three buttons (Cancel / Analyze / Generate Plan). Loading + review title + copy adapt to mode via `generateMode` state. Plan review drops the coaching_notes card entirely.
- `v2.2.6` — Hotfix: day-dropdown completion dot is accurate on hard reload. Pre-fix, the `●` next to a plan day only appeared after the user selected that day (triggering `loadHistorical(i)` which populated `historicalCache[i]`). On hard reload, `historicalCache` was only populated for the focused day, so every other plan day with prior workouts rendered without a dot despite having weeks of logged sets. Root cause: `buildTabs`'s dot check relied on `historicalCache[i]`, which is lazy-loaded per-selection. **Fix:** new `daysWithHistory` map populated by one cheap query at hydrate (`SELECT day_index FROM workouts WHERE user_id = X AND plan_id = activePlanId AND day_index IS NOT NULL`) — payload is a few ints. Dot check becomes `hasToday || daysWithHistory[i] || historicalCache[i]` (the last still checked as a fallback once selection populates it). Map is reset + reloaded on plan switch (`savePlanAsActive`, `activateExistingPlan`) and on sign-in. **Lesson:** when a UI element's correctness depends on lazy-loaded state, either eagerly load the minimal flag, or re-render on lazy-populate. Don't rely on the lazy-load happening "eventually" — that "eventually" is user-triggered.
- `v2.2.5` — Drag-to-reorder exercise cards. Long-press (400ms) on the card chrome → SortableJS lift → drop within the same sort zone. Two zones on plan days: prescribed exercises and ad-hoc extras (below the "Added exercises" divider); zones don't cross. Ad-hoc sessions get one zone. **Plan-zone reorder mutates `plan.data`** (same semantics as Swap v2.0.29) — toast: *"Reordered for the rest of the week. Plan updated."* **Extras / ad-hoc reorder is session-only** — toast: *"Reordered — this session only."* Current workout's sets are remapped so each set stays attached to its exercise across a drag. Set remap is two-phase (`+10000` temp shift then final position) to dodge transient collisions on the partial unique index `(workout_id, exercise_id, exercise_order, set_order) WHERE done = true`. `filter` excludes inputs/buttons/set-rows/RPE-row/sub-row so tapping those never starts a drag. SortableJS 1.15.2 via CDN; no schema changes.
- `v2.2.4` — Manual session duration adjustment. Small `✎` button next to every session duration display (running timer, completed-today, historical day view, ad-hoc running/completed, history detail). Tap → prompt for minutes (0-600 validated) → back-computes `started_at = (ended_at || now) - minutes*60000` so `sessionElapsedMs(state)` equals the requested span. `paused_ms` is zeroed — a manual override collapses any accumulated pause accounting. Context-aware refresh: today-context patches in-memory state (`todayState`, `todayPlanStates`, `todayAdHocs`) and re-renders; history-context invalidates `historyDetails` + week cache and re-opens the detail view. One helper (`renderDurationEditBtn`), one handler (`promptAdjustDuration`), two click-delegator hooks. No schema changes.
- `v2.2.3` — Hotfix for three v2.2.2 bugs surfaced during local-dev testing.
  - **Discard failed with "renderHistory not defined":** [js/ui.js:1195](js/ui.js#L1195) called `renderHistory()` instead of `renderHistoryWeek()` — DB delete actually landed, but the ReferenceError surfaced as a misleading *"couldn't discard session"* toast. Fix: typo corrected.
  - **"Bring to today" did nothing visible:** hydrate queries today's workouts by `performed_at` (timestamp), not `performed_on` (date). My v2.2.2 `reactivateWorkout` updated `performed_on=today` but left `performed_at` at the original value — hydrate filtered the reactivated workout out, `todayPlanStates` stayed empty, `inProgressKey` was null, and [app.js:143](js/app.js#L143)'s fallback opened the start-session modal. Fix: add `performed_at: now` to the UPDATE payload.
  - **PostgREST FK ambiguity (PGRST201) after v2.2.1 migration:** added `sets.prescribed_exercise_id` as a second FK from `sets` to `exercises`. Any PostgREST embed like `sets(*, exercises(...))` errored *"More than one relationship was found."* Broke [fetchWeekSummary](js/data.js#L449) (user-visible *"couldn't load week"*), [_fetchRecentExercisePerformance](js/data.js#L2095), [runExport](js/ui.js#L3145), and the Edge Function's [fetchRecentWorkouts](api/generate-plan.js#L307) (explains *"swap not working"* — swap mode shares this query). Fix: disambiguate each embed as `exercises!exercise_id(...)` to explicitly follow the actual-performed FK. Backward-compat with pre-migration installs (hint ignored when only one FK exists).
- `v2.2.2` — Session lifecycle recovery + swap regression fix + toast × dismiss. **Session lifecycle:** accidentally-started sessions that get paused and midnight-trapped can now be recovered. History detail modal gains two actions: *"Bring to today"* (moves the workout to today's date and resets the timer — preserves logged sets) and *"Discard session"* (deletes the workout and cascades sets). Reactivate is only offered when safe (ad-hoc, or plan-based on the currently-active plan). Today's in-progress plan-day session with 0 completed sets gets a dashed *"Cancel session (no sets logged)"* affordance under the session bar for the accidentally-started-before-midnight case. Partial unique index collision (23505) on reactivate returns a clear error (*"You already have this day started today"*). New `discardWorkout(id)` and `reactivateWorkout(id)` helpers in [js/data.js](js/data.js). **Swap regression fix:** v2.2.1's `updateExerciseFanOut` change was retargeting `exercise_id` + `prescribed_exercise_id` across *all* sets at the same exercise_order whenever RPE or note changed — this clobbered pre-swap sets that legitimately still belonged to the pre-swap exercise_id (v2.0.29 invariant: "already-logged sets on the replaced exercise are preserved"). Reverted fan-out to rpe + note only. Substitution retargeting moved into `logSubstitute` itself as a precise UPDATE filtered by current `exercise_id` — only sets that currently match the pre-change actual get retargeted; any legacy post-swap sets attached to a different exercise_id stay untouched. **Toast ×:** every toast now renders a small × close button. `stopPropagation` on the × click so dismissing a retry toast via × doesn't accidentally fire the retry callback.
- `v2.2.1` — Structured per-session substitutions. Replaces the v1 free-text SUB field with an exercise-library picker + structured DB tracking so substituted sets correctly file under the exercise actually performed (not the plan's prescribed one). **Schema:** new [20260420020000_prescribed_exercise_id.sql](supabase/migrations/20260420020000_prescribed_exercise_id.sql) adds `sets.prescribed_exercise_id uuid` — what the plan asked for — while `sets.exercise_id` becomes what actually happened. Backfills existing plan-day rows so `prescribed_exercise_id = exercise_id`. Partial index keyed to substitution rows (where prescribed != actual) for v2.2.2's AI substitution-pattern query. **UI:** SUB field on editable plan cards now opens the exercise picker (reuses the ad-hoc add picker via a new `openPicker(onSelect)` callback mechanism). Three states on the card: empty ("Substitute exercise…"), picked (substitute name + ✕ clear), legacy free-text (kept read-only + "tap to re-link" affordance until re-saved). When substituted, the card's **weight-mode label** tracks the substitute (per_side shows "/ea", bodyweight shows "ADD WT", etc.) rather than the prescribed exercise's mode — fixes a render bug that would have shown Cable Row's weight anchor on a Dumbbell Row substitute. Exercise name at the top of the card flips to the substitute with a subtle `was: <prescribed>` tag. Prescribed-weight placeholder clears when substituted (the plan's weight doesn't port across exercises); rep targets keep (they translate). **Scope clarity:** the Swap review modal gains a yellow callout — *"Accepting replaces this exercise in your plan for the rest of the week. For a one-session change, close this and use SUB on the card."* Swap accept toast says *"for the rest of the week. Plan updated."* Substitute apply toast says *"for today only. Plan unchanged — use the ⇄ Swap icon to change the week."* Makes the durable-vs-ephemeral split impossible to miss. **Coach integration:** `getLiveContext` labels substituted exercises inline (*"Cable Row → Machine Row [plan: 3×12 @120]"*) so Haiku sees both the prescription and what's actually being done. **Write path:** `buildSetPayload` writes `exercise_id = subExercise.id || prescribedId`; `prescribed_exercise_id = prescribedId` on plan-day sets, `null` on ad-hoc/extras. `updateExerciseFanOut` retargets the exercise_id across all already-persisted sets when substitution flips mid-session; in-memory slots stay in sync so re-renders reflect the change. Legacy `sets.substitution` free-text column untouched; new writes null it out. **Deferred to v2.2.2:** AI recommendation for weight/reps on the substitute (`mode: "substitute_recommend"` Edge Function branch), and a "substituted from X" annotation in View Recent.
- `v2.2.0` — Coach Chat. Real-time AI coaching available during a session (and outside one) via a floating chat button in the bottom-right. Model: Claude Haiku 4.5 (`claude-haiku-4-5-20251001`), 500-token budget, ~1-2s warm response. Context assembly is four layers: (Layer 1) inline system prompt in the endpoint, (Layer 2) semi-static summary built via `buildCoachContext()` on session start/complete/plan save — plan structure + this-week status + 2-week per-exercise history + recent session notes (~650 tokens typical), (Layer 3) live session state built fresh per message via `getLiveContext()` with inline plan prescription next to actuals so the coach can compare without cross-referencing, (Layer 4) in-memory `chatHistory` array trimmed to the last 20 messages. New endpoint [api/coach-chat.js](api/coach-chat.js) — separate from `/api/generate-plan` since model / budget / response contract all differ. System-prompt (1h cache) shared across chat calls. Chat history is **ephemeral by design** — ephemeral coaching maps to how the user actually operates between sets. Persistence is a candidate follow-up (see DECISIONS.md 2026-04-20 v2.2.0 entry). Auto-retry once on cold-start failures mirrors the v2.0.26 pattern. Lifecycle: session start / complete / plan save rebuild context + clear chat (non-blocking); sign-out wipes both. **Cron warmup constrained to daily by Vercel Hobby** — initial `*/5` / `*/10` cron config failed the v2.2.0 deploy (Hobby caps at once-per-day max). Reverted to `0 9 * * *` (9am UTC = 5am EDT / 4am EST) for both endpoints. Narrow benefit in practice — a daily ping keeps an instance warm for ~15-45 min; useful if the user trains within that window, useless otherwise. External pinger or Pro upgrade would unlock per-minute crons.
- `v2.1.0` — first v2.1 bundle: plan templates + AI exercise swap + View Recent enhancements. Shipped as three logical commits (retain the working v2.0.28 / v2.0.29 / v2.0.30 labels in git history) but surfaced to the user as v2.1.0 since this is the v2 → v2.1 milestone transition per ROADMAP. Three features in detail:
  - **Plan templates.** A template is a `plans` row with `is_template = true`, `is_active = false`, and a user-facing `template_name`. Users save a whole plan or a single day as a template, then use it to pre-populate an ad-hoc session later. Single hamburger entry **"Save as template"** (disabled when no active plan) opens a modal with a segmented scope picker — *Whole plan (N days)* vs *Just "[focused day name]"*. Day scope enabled only when focused on a plan day; hidden entirely when launched without day context (Plans-modal row). New hamburger entry **"Templates"** opens a management modal listing all templates with day count, created date, and Delete button. The start-session modal gets a **4th card "Use a template"** between Pick a different day and Blank session — tapping expands an inline list; single-day templates create the session immediately, multi-day templates expand inline to show day rows. `createAdHocFromTemplate` resolves each template exercise via `resolveLibraryRow` and pre-populates sets with weight + reps (done=false); exercises that can't be resolved are skipped with a toast. Plans-modal rows get a "Template" button to save historical plans. Migration [20260420010000_plan_templates.sql](supabase/migrations/20260420010000_plan_templates.sql) adds `is_template bool` + `template_name text` + a partial index keyed to template rows. Active-plan unique index is unaffected (templates always have `is_active = false`).
  - **AI exercise swap.** A small `⇄` icon on each plan exercise card (editable mode only, never on extras / ad-hoc) opens a modal that finds an AI-suggested replacement for that one exercise via `POST /api/generate-plan { mode: "swap", ... }`. Server early-dispatches to `handleSwap` with its own inline `SWAP_SYSTEM_PROMPT` (1h `cache_control` breakpoint, 500 max tokens, ~8-15s warm). `handleSwap` fetches active plan + 2-week history + library in parallel, builds a narrow user message (exercise snapshot, optional reason, day name, other-exercises-today, summarized movement-pattern history for weight calibration), and validates the reply (name in library, not equal to replaced, not already on this day, integer rest, non-empty sets). Frontend modal follows the same input / loading / review three-view pattern as Generate. On Accept: mutates `plan.days[di].exercises[ei]` in place and writes `plans.data` to Supabase. `originalExercise` deep-clone captured at modal-open time lets the error path revert in-memory cleanly so UI never drifts from DB. Already-logged sets on the replaced exercise stay attached to their original `exercise_id` in the `sets` table — they become historical for the old exercise on that one workout.
  - **View Recent display enhancements.** `openExerciseHistory` now surfaces two new signals per historical session. **Exercise notes:** a muted-italic line below RPE when the user logged a per-exercise note on that day. All sets of one exercise-on-workout share the same note value (buildSetPayload writes `exState.note` to every set), so the display picks the first non-empty note per session; no empty placeholders for workouts without a note. **Gym location** was already being joined via `workouts.location_id → locations.name`; added an `@` prefix to the tag for clarity ("@ Home Gym"). Final layout when all signals present: `MON, APR 14 · DAY 3 — ARMS · @ HOME GYM` / sets / `RPE 8` / `grip failing on set 3`.

**AI plan generation architecture (live):**
- Serverless function at [api/generate-plan.js](api/generate-plan.js) deployed on Vercel, called from the frontend via `POST /api/generate-plan` with an `Authorization: Bearer <supabase-jwt>` header.
- JWT verified against Supabase `/auth/v1/user`; service-role key used for all downstream DB queries (bypasses RLS, but we still filter by `user_id` defensively).
- Data gathered: active plan, last 4 weeks of workouts (with sets + exercise taxonomy), full exercise library, latest goal + progress photo.
- System prompt lives in [system-prompt.md](system-prompt.md) at repo root, bundled into the function via [vercel.json](vercel.json) `includeFiles`. `fs.readFileSync` at cold start.
- Prompt caching: **2 breakpoints, both 1h ephemeral TTL** — one on system prompt, one on exercise library. Cache key = system + library (~8500 tokens). Every subsequent call within the hour hits the cache, cutting input cost ~90%.
- `repeat: N` shorthand on set objects: Claude emits one set object with `repeat: 3` when all sets of an exercise are identical; `expandSetRepeats` unrolls server-side so the frontend contract is unchanged. Cuts output tokens ~60%.
- Client-side 55s `AbortController` timeout (under Vercel Hobby's 60s cap). Cancel button during loading view, Cancel + Accept during review view. Closing the modal via × or overlay-click during an in-flight fetch aborts it — prevents orphaned requests.
- Verified live at 22-30s per generation (cache hit), ~$0.035 per run on Sonnet 4.6.

**Non-obvious design choices worth reviewing:** `paused_ms` semantics, the `(user_id, plan_id, day_index, performed_on)` unique-index, the Sonnet-timeout root cause (instruction-text in prompts causes longer reasoning), and the cached-USER-INPUTS pattern — all in [DECISIONS.md](DECISIONS.md). The Sonnet latency debugging narrative is in [MAJOR-BUGS.md](MAJOR-BUGS.md).

**Working conventions established during Session A** (preserve these in the next session):
- Every visible change bumps `APP_VERSION` (`v2.0.x` per iteration). Keeps stale-cache diagnosis trivial.
- User tests before every commit unless explicitly told otherwise. Commits are small and focused — avoid bundling unrelated work.
- All migrations go in `supabase/migrations/` with the Supabase timestamp convention; never edit an applied migration — write a forward-only one.
- Error toasts with retry callbacks are sticky until tapped; informational toasts auto-dismiss at 20s.
- Destructive operations get explicit user confirmation; pushes require explicit user approval.

## Decisions locked in (original v1 — for context)

1. **Plans stay as `jsonb`** — the plan structure rarely needs set-level queries.
2. **Workouts + sets are normalized.** One row per session, one row per set.
3. **Per-set RPE**, not per-exercise (standard input for fatigue/progression models).
4. **Exercises table with FK** from sets, not free-text names (prevents history fragmentation like "Bench" vs "Barbell Bench Press").
5. **Prescribed and actual columns on `sets`** so "did I hit the target" is queryable without rejoining the plan blob.
6. **Write strategy: save-on-set-completion.** Typing weight/reps/RPE updates local state only. Tapping the "done" checkbox upserts that one set's row. Each network request = one complete set. Handles flaky gym wifi; matches sets as the atomic unit.
7. **RLS on every table**: `auth.uid() = user_id`.

## Proposed schema

```sql
create table plans (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  week text,
  data jsonb not null,
  is_active boolean not null default false,
  created_at timestamptz not null default now()
);
create index on plans (user_id, created_at desc);

create table exercises (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  name text not null,
  created_at timestamptz not null default now(),
  unique (user_id, name)
);

create table workouts (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  plan_id uuid references plans(id) on delete set null,
  day_index int,
  performed_at timestamptz not null default now(),
  started_at timestamptz,
  ended_at timestamptz,
  notes text
);
create index on workouts (user_id, performed_at desc);

create table sets (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  workout_id uuid not null references workouts(id) on delete cascade,
  exercise_id uuid not null references exercises(id),
  exercise_order int not null,
  set_order int not null,
  weight numeric,
  reps int,
  rpe int,
  prescribed_weight numeric,
  prescribed_reps int,
  substitution text,
  note text,
  done boolean not null default false,
  started_at timestamptz,
  completed_at timestamptz,
  constraint sets_completed_at_required_when_done
    check (done = false or completed_at is not null)
);
create index on sets (user_id, exercise_id, completed_at desc);
create index on sets (workout_id, exercise_order, set_order);
create index on sets (workout_id, completed_at);

alter table plans     enable row level security;
alter table exercises enable row level security;
alter table workouts  enable row level security;
alter table sets      enable row level security;

create policy "own_plans"     on plans     for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own_exercises" on exercises for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own_workouts"  on workouts  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "own_sets"      on sets      for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

Indexes target three expected query shapes: "all sets for exercise X over time," "all sets in workout Y in canonical sequence," and "all sets in workout Y in time order" (for rest-interval/duration analysis).

**Timestamps on sets and workouts.** `sets.started_at` is set when weight or reps is first entered; `completed_at` is set (and enforced via CHECK) when `done` flips true; the app defaults it to `now()` at that moment. `workouts.started_at` is set on the first touch of any set in the session; `workouts.ended_at` is set when the user explicitly ends the workout, otherwise defaults to the last set's `completed_at`. Order fields (`exercise_order`, `set_order`) remain the canonical sequence — timestamps are the source of truth for *when*, order fields for *sequence*.

## Gotchas / lessons learned

- **Local dev browser caching.** Restarting the Python server doesn't invalidate the browser cache, so the frontend can be running stale JavaScript against a fresh server. Always hard-reload (Cmd+Shift+R) after restarting the local server — normal reload (Cmd+R) can serve cached files and silently mask a fix.
- **`vercel dev` doesn't hot-reload system-prompt.md.** The prompt is loaded via `fs.readFileSync` at module cold start. Editing the file requires killing and restarting `vercel dev` to pick up changes; otherwise the old prompt stays in memory and Anthropic keeps hitting the old cache entry.
- **`vercel dev` serializes local function invocations.** Concurrent calls (UI + console test at the same time) stack; second call hangs until first completes. In production (real Vercel) each invocation spawns its own instance, so this is dev-only.
- **Commit milestones as they pass their test gates.** When a multi-step change has clear internal milestones, commit each milestone as it's verified rather than holding the whole stack uncommitted.
- **Instruction text in prompts has real latency cost.** See [MAJOR-BUGS.md](MAJOR-BUGS.md) (2026-04-20). A single "think carefully about X" sentence in the user message can add ~1000 output tokens and 6-10s of generation time. Put rules in the cached system prompt; put data in dynText.

## Known v1 limitations

**Open:**
- **Orphan user-custom exercise rows from pre-`v2.0.10` write path (mechanism still exists; user-specific cleanup done 2026-04-19).** Before the resolver landed, every `ensureExerciseId(planName)` call created a user-custom row under the raw-normalized plan name (e.g. `"pull-ups (bw, full rom)"`, `"db romanian deadlift"`) and logged sets against it. `loadExerciseLibrary` pulls everything RLS exposes — seed + user-custom — so these orphan rows are still in `exerciseLibraryByName` and can intercept `resolveLibraryRow` before it reaches the canonical seed candidate, because raw-normalized candidates are tried first. Visible symptom: `openExerciseHistory` for an affected plan exercise shows only recent plan-logged sets (on the orphan row) while Fitbod/historical imports on the seed row stay hidden. The current user's two orphans (`db romanian deadlift`, `db bench press`) were merged + deleted on 2026-04-19 (see `MAJOR-BUGS.md`). The resolver-ordering mechanism that lets orphans win over aliases is still present; a more durable fix is scheduled as "resolve at plan-import time" in `ROADMAP.md`. Fix recipe for any future orphan: `UPDATE sets SET exercise_id = <canonical id> WHERE exercise_id = <orphan id>` then `DELETE FROM exercises WHERE id = <orphan id>`. A Supabase SQL audit to find orphans for the active user:
  ```sql
  SELECT id, name, user_id, is_custom
  FROM exercises
  WHERE user_id = '<uid>' AND is_custom = true
  ORDER BY name;
  ```
  Compare each entry against the seed library and the alias map; rows matching a plan exercise name should be merged into their canonical counterpart. Decline Crunch / VO2 Max Intervals / Zone 2 Walk / Cable Woodchop (if no seed "woodchop") genuinely have no canonical target — leave them.

**Resolved in Session A:**
- Multi-tab / first-insert retry dup → partial unique index on `(user_id, plan_id, day_index, performed_on)` + client 23505 re-query. See `DECISIONS.md`.
- Midnight boundary drift → `sessionTodayStart` snapshotted at hydrate, used by everything via `sessionBounds()` / `sessionTodayDateString()`.
- One editable tab per calendar day → `todayPlanStates` is now a map keyed by dayIndex, so every plan day tab independently tracks its own today-workout.
- Plan-name vs seed-library name divergence creating split history → resolved in `v2.0.10` by `resolveLibraryRow` + `EXERCISE_ALIASES`. Orphan rows from pre-`v2.0.10` persists remain a data cleanup task (see "Open" above), but new plan-logged sets land on the canonical row.

## Pre-deploy tasks

- *(No open pre-deploy tasks — see "Done" for the SMTP resolution.)*

## Done

- **Custom SMTP provider (2026-04-15).** Resend configured in Supabase → Authentication → Emails using the shared `onboarding@resend.dev` sender. Unblocks magic-link testing. Free tier limits are 3,000 emails/month and 100/day — well beyond expected usage, but worth knowing if this ever scales up. Upgrading to a custom domain sender is optional polish for post-v1.

## Deferred features (post-v2)

Most v1 deferred items have shipped. Remaining scope lives in [ROADMAP.md](ROADMAP.md) — notably: chat-based mid-week coaching, automatic deload detection, PR detection/progression charts, replan-remainder mode, side-by-side photo comparison with AI commentary, configurable AI context window, Vercel Pro upgrade for longer-generation headroom.

## Non-goals still

- Multi-user features (RLS is single-user-scoped; no sharing/access layer).
- Conflict resolution beyond last-write-wins (single user, single device at a time in practice).
- Full plan editing in-app (accept / import / activate are the edit surface; finer-grained editing lives in regenerate-or-re-import).
